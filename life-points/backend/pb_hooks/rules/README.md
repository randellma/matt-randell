# Collection rules

Keep every private collection's Game-scoped PocketBase API rules here as the schema arrives.
The walking skeleton has no Account, Game, or private collection yet, so there are no rules to
install in this slice. Collection migrations remain the source of truth for the rules that are
ultimately applied to PocketBase.
