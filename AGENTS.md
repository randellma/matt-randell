## Agent skills

### Issue tracker

Issues live as markdown files in `docs/issues/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five canonical triage role strings (no customisation). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` at root with `docs/adr/`. See `docs/agents/domain.md`.
