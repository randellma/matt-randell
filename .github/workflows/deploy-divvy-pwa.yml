name: Deploy Divvy PWA

on:
  push:
    branches: [main]
    paths:
      - divvy/web/**
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: divvy/web/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: divvy/web
      - name: Test
        run: npm test
        working-directory: divvy/web
      - name: Typecheck
        run: npm run typecheck
        working-directory: divvy/web
      - name: Build
        run: npm run build
        working-directory: divvy/web
      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name divvy-mattrandell --branch main
        working-directory: divvy/web
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_PAGES_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: 894ff489298bdf1ca445fc9469854b25
